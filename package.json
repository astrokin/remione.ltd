{
  "name": "remione-ltd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "firebase deploy --only hosting --project remione-ltd --non-interactive",
    "catalog:sync": "node scripts/sync-catalog.mjs",
    "test": "node --test tests/*.test.mjs",
    "release:prepare": "node scripts/prepare-publish.mjs",
    "release:verify": "node scripts/verify-release.mjs"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "firebase-tools": "^14.0.0",
    "google-play-scraper": "10.1.2",
    "image-size": "2.0.2",
    "vite": "^7.0.0"
  }
}
